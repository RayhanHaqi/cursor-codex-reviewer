# Documentation-only example for manual environment setup.
# The /call-codex skill does NOT automatically source this file or CODEX_REVIEW_ENV_FILE.
# Launch Cursor from an environment where codex is on PATH and variables are already configured.
# Never commit real secrets to version control.

# Example placeholder variables only:
# CODEX_HOME=~/.codex
# CONTEXT7_API_KEY=your-key-here